local OrionLib = loadstring(game:HttpGet(('https://raw.githubusercontent.com/BlizTBr/scripts/main/Orion%20X')))()

local Window = OrionLib:MakeWindow({Name = "Verbal Hub FTAP made by JESSETC92",HidePremium = false,IntroEnabled = true,
    IntroText = "Welcome To Verbal Hub LOL",
    IntroIcon = "rbxassetid://18526520539", SaveConfig = true, IntroEnabled = true, ConfigFolder = "OrionTest"})
local Tab = Window:MakeTab({

    Name = "Main",
    Icon = "rbxassetid://5191098772",
    PremiumOnly = false
})

Tab:AddColorpicker({
	Name = "Colorpicker",
	Default = Color3.fromRGB(255,255, 255),
	Callback = function(Value)
		print(Value)
	end	  
})

local Section = Tab:AddSection({
    Name = "Instagram Yng_J0sed1 owner JESETC92 -GLOCKZ"
})

local Tab1 = Window:MakeTab({
    Name = "Grabs",
    Icon = "rbxassetid://4483345998",
    PremiumOnly = false
})

local Tab2 = Window:MakeTab({
    Name = "Blobman",
    Icon = "rbxassetid://4483345998",
    PremiumOnly = false
})

local Tab3 = Window:MakeTab({
    Name = "SpyChat",
    Icon = "rbxassetid://4483345998",
    PremiumOnly = false
})

Tab:AddButton({
	Name = "Anti-Grab",
	Callback = function()
        while true do
            local hui = game:GetService("Players").LocalPlayer
            game:GetService("ReplicatedStorage").CharacterEvents.Struggle:FireServer(hui)
            wait(0.1)
            end
  	end    
})

Tab:AddButton({
	Name = "Anti everything",
	Callback = function()
      		print("button pressed")
              loadstring(game:HttpGet("https://pastebin.com/raw/3abHLtrc",true))()
  	end    
})

Tab:AddButton({
	Name = "Anti-Explosion",
	Callback = function()
      		print("button pressed")
              workspace.DescendantAdded:Connect(function(v)
                if v:IsA("Explosion") then
                    v.BlastPressure = 1200
                end
             end)
  	end    
})

Tab:AddButton({
	Name = "Anti Burn soon",
	Callback = function()
      		print("button pressed")
   
  	end    
})

Tab:AddToggle({
	Name = "Anti Void",
	Default = false,
	Callback = function(Value)
		print(Value)
        -- Anti-Void Lua Script for Fling Things And People Game

-- Function to check if an object is about to fall into the void
function checkAntiVoid(object)
    local objectPosition = object.Position
    local antiVoidPosition = Vector3.new(0, 50, 0) -- Adjust the position based on the game's void location
    local distance = (objectPosition - antiVoidPosition).Magnitude
    
    if distance < 10 then
        -- Prevent falling into the void
        object.CFrame = CFrame.new(objectPosition) -- Reset the object's position to prevent falling
    end
end

-- Function to continuously check and prevent objects from falling into the void
function antiVoidUpdate()
    for _, object in pairs(workspace:GetChildren()) do
        if object:IsA("Model") then
            checkAntiVoid(object)
        end
    end
end

-- Connect the antiVoidUpdate function to the game loop
game:GetService("RunService").Heartbeat:Connect(antiVoidUpdate)

-- Remember to adjust positions, distances, and other parameters as needed for the "Fling Things And People" game
	end    
})

--functions
Tab1:AddSlider({
    Name = "speed",
    Min = 16,
    Max = 1000,
    Default = 5,
    Color = Color3.fromRGB(255,255,255),
    Increment = 1,
    ValueName = "speed",
    Callback = function(speed)
        HackedWalkSpeed = speed

        local Plrs = game:GetService("Players")
        local MyPlr = Plrs.LocalPlayer
        local MyChar = MyPlr.Character

        if MyChar then
            local Hum = MyChar.Humanoid
            Hum.Changed:connect(function()
                Hum.WalkSpeed = HackedWalkSpeed
            end)
            Hum.WalkSpeed = HackedWalkSpeed
        end

        MyPlr.CharacterAdded:connect(function(Char)
            MyChar = Char
            repeat wait() until Char:FindFirstChild("Humanoid")
            local Hum = Char.Humanoid
            Hum.Changed:connect(function()
                Hum.WalkSpeed = HackedWalkSpeed
            end)
            Hum.WalkSpeed = HackedWalkSpeed
        end)
    end
})

--КИК РУКОЙ
getgenv().CanKick = false;

local function KickPlayer()
    local player = game:GetService("Players").LocalPlayer

    local function KickEnabler(character)
        local humanoidRootPart = character:FindFirstChild("HumanoidRootPart")
        local ragdollTouchedHitbox = humanoidRootPart and humanoidRootPart:FindFirstChild("RagdollTouchedHitbox")
        if ragdollTouchedHitbox then
            ragdollTouchedHitbox.CanQuery = CanKick
            ragdollTouchedHitbox.CanCollide = CanKick
            ragdollTouchedHitbox.Size = CanKick and Vector3.new(4.875, 6.625, 3.75) or Vector3.new(2.875, 3.625, 1.75)
        end
    end

    local function ApplyKickToPlayers()
        for _, otherPlayer in ipairs(game:GetService("Players"):GetPlayers()) do
            if otherPlayer ~= player and otherPlayer.Character then
                KickEnabler(otherPlayer.Character)
            end
        end
    end

    local function CheckForPlayers()
        while true do
            ApplyKickToPlayers()
            wait(0.01)
        end
    end

    player.CharacterAdded:Connect(function(character)
        KickEnabler(character)
    end)

    game:GetService("Players").PlayerAdded:Connect(function(newPlayer)
        newPlayer.CharacterAdded:Connect(function(character)
            if newPlayer ~= player then
                KickEnabler(character)
            end
        end)
    end)

    if player.Character then
        KickEnabler(player.Character)
    end

    spawn(CheckForPlayers)

    if CanKick then
        print("Kicking enabled")
    else
        print("Kicking disabled")
    end
end


Tab1:AddToggle({
    Name = "Kick with Line (Beta)",
    Default = false,
    Callback = function(Value)
        CanKick = Value
    end
})

KickPlayer()

Tab:AddLabel("Use KeyBind T to bring object u can get banned aim at the item")

Tab:AddButton({
	Name = "bring objects close buggy",
	Callback = function()
      		print("button pressed")
              local Players = game:GetService("Players")
              local UserInputService = game:GetService("UserInputService")
              
              local isScriptRunning = false
              
              -- Function to get a random part in the game workspace
              local function getRandomPart()
                  local parts = game.Workspace:GetDescendants()
                  local randomPart = parts[math.random(1, #parts)]
                  if randomPart and randomPart:IsA("BasePart") then
                      return randomPart
                  end
                  return nil
              end
              
              -- Function to allow a player to grab a random part
              local function grabRandomPart(player)
                  local randomPart = getRandomPart()
                  if randomPart then
                      local humanoidRootPart = player.Character:FindFirstChild("HumanoidRootPart")
                      if humanoidRootPart then
                          randomPart.CFrame = humanoidRootPart.CFrame * CFrame.new(0, 0, -5)  -- Adjust distance as needed
                      end
                  end
              end
              
              -- Function to apply random grabbing for all players in the game
              local function randomGrabForAllPlayers()
                  for _, player in pairs(Players:GetPlayers()) do
                      grabRandomPart(player)
                  end
              end
              
              -- Function to toggle script execution
              local function toggleScriptExecution()
                  isScriptRunning = not isScriptRunning
                  if isScriptRunning then
                      randomGrabForAllPlayers()
                  end
              end
              
              -- Listen for key input to start and stop the script
              UserInputService.InputBegan:Connect(function(input, isProcessed)
                  if input.KeyCode == Enum.KeyCode.T and not isProcessed then
                      toggleScriptExecution()
                  end
              end)
  	end    
})

local grabbingScriptEnabled = false

local function ToggleGrabbingScript(enabled)
    grabbingScriptEnabled = enabled
    local grabbingScript = game.Players.LocalPlayer.Character and game.Players.LocalPlayer.Character:FindFirstChild("GrabbingScript", true)
    if grabbingScript then
        grabbingScript.Disabled = not enabled
    end
end

local function OnCharacterAdded(character)
    if not grabbingScriptEnabled then
        ToggleGrabbingScript(false)
    end
end

if game.Players.LocalPlayer.Character then
    OnCharacterAdded(game.Players.LocalPlayer.Character)
end

game.Players.LocalPlayer.CharacterAdded:Connect(OnCharacterAdded)

Tab1:AddToggle({
    Name = "Control Grab Script",
    Default = true,
    Callback = function(enabled)
        ToggleGrabbingScript(enabled)
    end
})

Tab:AddLabel("Read This To use kill grab Press key f to stop it press f again")

Tab:AddButton({
	Name = "Kill Grab",
	Callback = function()
      		print("button pressed")
              local Players = game:GetService("Players")
              local UserInputService = game:GetService("UserInputService")
              
              local isScriptRunning = false
              local intervalInSeconds = 4  -- Adjust the interval as needed
              
              local function annoyPlayer(player)
                  local character = player.Character
                  if character then
                      local humanoid = character:FindFirstChild("Humanoid")
                      if humanoid then
                          humanoid.PlatformStand = true
                          humanoid.Jump = false
                          humanoid.WalkSpeed = 0
                          humanoid.Health = 0
                          wait(0)
                          humanoid.PlatformStand = false
                          humanoid.Jump = false
                          humanoid.WalkSpeed = 16
                          humanoid.Health = 100
                          humanoid.RootPart.Velocity = Vector3.new(math.random(-50, 50), math.random(50, 100), math.random(-50, 50))
                      end
                  end
              end
              
              local function annoyAllPlayers()
                  for _, player in pairs(Players:GetPlayers()) do
                      annoyPlayer(player)
                  end
              end
              
              local function toggleScriptExecution()
                  isScriptRunning = not isScriptRunning
                  if isScriptRunning then
                      while isScriptRunning do
                          wait(intervalInSeconds)
                          annoyAllPlayers()
                      end
                  end
              end
              
              UserInputService.InputBegan:Connect(function(input, isProcessed)
                  if not isProcessed then
                      if input.KeyCode == Enum.KeyCode.F then  -- Change the KeyCode to the desired key
                          toggleScriptExecution()
                      end
                  end
              end)
  	end    
})

Tab:AddButton({
	Name = "Noclip grab",
	Callback = function()
      		print("button pressed")
              local Players = game:GetService("Players")
              local UserInputService = game:GetService("UserInputService")
              
              local isNoclipEnabled = false
              
              UserInputService.InputBegan:Connect(function(input)
                  if input.KeyCode == Enum.KeyCode.LeftControl then
                      isNoclipEnabled = not isNoclipEnabled
                      game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").Anchored = isNoclipEnabled
                  end
              end)
              
              UserInputService.InputEnded:Connect(function(input)
                  if input.KeyCode == Enum.KeyCode.LeftControl then
                      if isNoclipEnabled then
                          isNoclipEnabled = false
                          game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").Anchored = isNoclipEnabled
                      end
                  end
              end)
              
              script.Parent.MouseButton1Click:Connect(function()
                  if isNoclipEnabled then
                      local character = game.Players.LocalPlayer.Character
                      local root = character:WaitForChild("HumanoidRootPart")
                      
                      local ray = Ray.new(root.Position, (game.Workspace.CurrentCamera.CFrame.Position - root.Position).unit * 1000)
                      local part, position = workspace:FindPartOnRay(ray, character)
                      
                      if part and part:IsDescendantOf(character) then
                          character:MoveTo(position)
                      end
                  end
              end)
  	end    
})

local Section = Tab:AddSection({
	Name = "M to Levitate kinda like anchor C to un levitate Z to hide line when use levitate"
})

Tab:AddButton({
	Name = " levitate grabs",
	Callback = function()
      		print("button pressed")
              loadstring(game:HttpGet('https://pastebin.com/raw/ZbU5bMxF'))()
  	end    
})


local Section = Tab3:AddSection({
	Name = "turn SpyChat on and off"
})

Tab3:AddToggle({
    Name = "Spy",
    Default = false,
    Callback = function(Value)
        local enabled = Value
        local spyOnMyself = true 
        local public = false 
        local publicItalics = true 
        local privateProperties = { 
            Color = Color3.fromRGB(0, 179, 255), 
            Font = Enum.Font.SourceSans,
            TextSize = 16,
        }

        local StarterGui = game:GetService("StarterGui")
        local Players = game:GetService("Players")
        local player = Players.LocalPlayer or Players:GetPropertyChangedSignal("LocalPlayer"):Wait() or Players.LocalPlayer
        local saymsg = game:GetService("ReplicatedStorage"):WaitForChild("DefaultChatSystemChatEvents"):WaitForChild("SayMessageRequest")
        local getmsg = game:GetService("ReplicatedStorage"):WaitForChild("DefaultChatSystemChatEvents"):WaitForChild("OnMessageDoneFiltering")
        local instance = (_G.chatSpyInstance or 0) + 1
        _G.chatSpyInstance = instance

        local function onChatted(p, msg)
            if _G.chatSpyInstance == instance then
                if p == player and msg:lower():sub(1, 4) == "/spy" then
                    enabled = not enabled
                    wait(0.3)
                    privateProperties.Text = "{SECRET " .. (enabled and "EN" or "DIS") .. "ABLED}"
                    StarterGui:SetCore("ChatMakeSystemMessage", privateProperties)
                elseif enabled and (spyOnMyself == true or p ~= player) then
                    msg = msg:gsub("[\n\r]", ''):gsub("\t", ' '):gsub("[ ]+", ' ')
                    local hidden = true
                    local conn = getmsg.OnClientEvent:Connect(function(packet, channel)
                        if packet.SpeakerUserId == p.UserId and packet.Message == msg:sub(#msg - #packet.Message + 1) and (channel == "All" or (channel == "Team" and public == false and Players:GetPlayerFromCharacter(packet.Character) and Players:GetPlayerFromCharacter(packet.Character).Team == player.Team)) then
                            hidden = false
                        end
                    end)
                    wait(0.25) -- Reduced the wait time to 0.25 seconds
                    conn:Disconnect()
                    if hidden and enabled then
                        if public then
                            saymsg:FireServer((publicItalics and "/me " or '') .. "{SECRET} [" .. p.Name .. "]: " .. msg, "All")
                        else
                            privateProperties.Text = "{SECRET} [" .. p.Name .. "]: " .. msg
                            StarterGui:SetCore("ChatMakeSystemMessage", privateProperties)
                        end
                    end
                end
            end
        end

        for _, p in ipairs(Players:GetPlayers()) do
            p.Chatted:Connect(function(msg) onChatted(p, msg) end)
        end

        Players.PlayerAdded:Connect(function(p)
            p.Chatted:Connect(function(msg) onChatted(p, msg) end)
        end)

        privateProperties.Text = "{SECRET " .. (enabled and "EN" or "DIS") .. "ABLED}"
        StarterGui:SetCore("ChatMakeSystemMessage", privateProperties)

        if not player.PlayerGui:FindFirstChild("Chat") then
            wait(3)
        end

        local chatFrame = player.PlayerGui.Chat.Frame
        chatFrame.ChatChannelParentFrame.Visible = true
        chatFrame.ChatBarParentFrame.Position = chatFrame.ChatChannelParentFrame.Position + UDim2.new(UDim.new(), chatFrame.ChatChannelParentFrame.Size.Y)
    end    
})


local Tab = Window:MakeTab({
	Name = "Jump Movement",
	Icon = "rbxassetid://4483345998",
	PremiumOnly = false
})

local Section = Tab:AddSection({
	Name = "jumppower"
})

Tab:AddSlider({
	Name = "JumpPower",
	Min = 20,
	Max = 10000,
	Default = 15,
	Color = Color3.fromRGB(255,255,255),
	Increment = 1,
	ValueName = "power",
	Callback = function(Value)
		print(Value)
        game.Players.LocalPlayer.Character.Humanoid.JumpPower = Value
	end    
})

local Tab = Window:MakeTab({
	Name = "Stronger Throws",
	Icon = "rbxassetid://4483345998",
	PremiumOnly = false
})

Tab:AddButton({
	Name = "Stronger Throws if needed",
	Callback = function()
      		print("button pressed")
              local bodyvel_Name = "FlingVelocity"
              local userinputs = game:GetService("UserInputService")
              local w = game:GetService("Workspace")
              local r = game:GetService("RunService")
              local d = game:GetService("Debris")
              local strength = 350
              
              w.ChildAdded:Connect(function(model)
                  if model.Name == "GrabParts" then
                      local part_to_impulse = model["GrabPart"]["WeldConstraint"].Part1
              
                      if part_to_impulse then
                          print("Part found!")
              
                          local inputObj
                          local velocityObj = Instance.new("BodyVelocity", part_to_impulse)
                          
                          model:GetPropertyChangedSignal("Parent"):Connect(function()
                              if not model.Parent then
                                  if userinputs:GetLastInputType() == Enum.UserInputType.MouseButton2 then
                                      print("Launched!")
                                      velocityObj.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
                                      velocityObj.Velocity = workspace.CurrentCamera.CFrame.lookVector * strength
                                      d:AddItem(velocityObj, 1)
                                  elseif userinputs:GetLastInputType() == Enum.UserInputType.MouseButton1 then
                                      velocityObj:Destroy()
                                      print("Cancel Launch!")
                                  else
                                      velocityObj:Destroy()
                                      print("No two keys pressed!")
                                  end
                              end
                          end)
                      end
                  end
              end)
  	end    
})

local Section = Tab:AddSection({
    Name = "Script made by JESSETC92"
})

local Tab = Window:MakeTab({
	Name = "aimbot/aimlock",
	Icon = "rbxassetid://4483345998",
	PremiumOnly = false
})

Tab:AddButton({
	Name = "AimLock",
	Callback = function()
      		print("button pressed")
              loadstring(game:HttpGet("https://pastebin.com/raw/2iCn5Tee",true))()
  	end    
})

local Section = Tab:AddSection({
	Name = "Right mouse button hold Activates Aimlock"
})

Tab:AddButton({
	Name = "ftap Aimlock",
	Callback = function()
      		print("button pressed")
              loadstring(game:HttpGet("https://github.com/xChonkster/RobloxScripts/blob/main/Misc/aimsp%20v2.lua?raw=true", true))()
  	end    
})

local Tab = Window:MakeTab({
	Name = "MISC",
	Icon = "rbxassetid://463049246",
	PremiumOnly = false
})

Tab:AddButton({
    Name = "Make Fov normal",
    Callback = function()
        print("Button pressed")
        local Camera = game.Workspace.CurrentCamera
        Camera.FieldOfView = 80
    end
})

Tab:AddSlider({
	Name = "Fov Changer",
	Min = 10,
	Max = 200,
	Default = 80,
	Color = Color3.fromRGB(255,255,255),
	Increment = 1,
	ValueName = "bananas",
	Callback = function(Value)
		print(Value)
		-- Add your FOV adjustment logic here using the 'Value' parameter
		-- For example, you can set the FOV of the camera using the Value like so:
		game.Workspace.CurrentCamera.FieldOfView = Value
	end
})

Tab:AddButton({
	Name = "Fake Ban SAFE",
	Callback = function()
      		print("button pressed")-- Annoyance Script for Fling Things and People Game
              -- This script will irritate other players in the game
              
              local Players = game:GetService("Players")
              
              local function annoyPlayers()
                  for _, player in pairs(Players:GetPlayers()) do
                      player:Kick("Thanks For Using Verbal hub You Used fake ban")
                      wait(5) -- Wait for 5 seconds before flinging again
                  end
              end
              
              -- Call the annoyPlayers function every 30 seconds
              while true do
                  annoyPlayers()
                  wait(30) -- Wait for 30 seconds before annoying players again
              end
  	end    
})


local Section = Tab:AddSection({
	Name = "GALAXY THROWS IF NEEDED"
})

Tab:AddButton({
	Name = "THROWS U TO THE GALAXY⚠️⚠️",
	Callback = function()
      		print("HEY DONT DO THAT!!")
              loadstring(game:HttpGet("https://pastebin.com/raw/m6CMDHYJ",true))()
  	end    
})

Tab:AddLabel("Gos to a random Player You Can Only See There Pov and annoy them by spawning items")

Tab:AddButton({
	Name = "Control player",
	Callback = function()
      		print("button pressed")
              local Players = game:GetService("Players")
              local function AnnoyPlayers()
                  for _,player in pairs(Players:GetPlayers()) do
                      local character = player.Character
                      if character then
                          local humanoid = character:FindFirstChild("Humanoid")
                          if humanoid then
                              humanoid.WalkSpeed = 100 -- Increase the player's walk speed
                              game.Workspace.CurrentCamera.CameraSubject = character -- Follow the targeted player
                              game.Workspace.CurrentCamera.CameraType = Enum.CameraType.Track -- Track the player's movements
                          end
                      end
                  end
              end
              
              AnnoyPlayers()
  	end    
})

Tab:AddButton({
	Name = "Posion Grab",
	Callback = function()
      		print("button pressed")
              loadstring(game:HttpGet("https://pastebin.com/raw/iUg4Lw2p",true))()
  	end    
})
local Section = Tab:AddSection({
	Name = "Made by ItsJose-_- DONT GRAB ITEMS USING THIS WILL TAKE ALL THE TOYS"
})

Tab:AddButton({
	Name = "no longer works",
	Callback = function()
      		print("button pressed")
              loadstring(game:HttpGet("https://pastebin.com/raw/N1SehJ12",true))()
  	end    
})


Tab:AddLabel("Aim Ur Cursor To Anyone And Use Keybind Q to teleport To Them")

Tab:AddButton({
	Name = "Annoy People WORKING",
	Callback = function()
      		print("button pressed")
            -- Silent Aim Lua Script for Fling Things and People on Solara
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer

local function getClosestPlayer()
    local ClosestDistance = math.huge
    local ClosestPlayer = nil
    for i, Player in ipairs(Players:GetPlayers()) do
        if Player ~= LocalPlayer and Player.Character and Player.Character:FindFirstChild("HumanoidRootPart") then
            local Distance = (Player.Character.HumanoidRootPart.Position - LocalPlayer.Character.HumanoidRootPart.Position).magnitude
            if Distance < ClosestDistance then
                ClosestDistance = Distance
                ClosestPlayer = Player
            end
        end
    end
    return ClosestPlayer
end

local function silentAim()
    local Target = getClosestPlayer()
    if Target and Target.Character and Target.Character:FindFirstChild("HumanoidRootPart") then
        LocalPlayer.Character.HumanoidRootPart.CFrame = Target.Character.HumanoidRootPart.CFrame
    end
end

while true do
    wait(0.1) -- Adjust the delay here for smoother or faster aiming
    silentAim()
end
  	end    
})

Tab:AddButton({
	Name = "buggy",
	Callback = function()
      		print("button pressed")
              game:GetService("Workspace").ChildAdded:Connect(function (model)
                if model.Name == "GrabParts" then
                local part_to_impulse = model["GrabPart"]["WeldConstraint"].Part1
                if part_to_impulse then
                part_to_impulse.Parent:BreakJoints()	
                end	
                end	
                 
                end)
  	end    
})

local Section = Tab:AddSection({
	Name = "I recommen using this instant kill working"
})

Tab:AddButton({
	Name = "Infinite Yield",
	Callback = function()
      		print("button pressed")
              loadstring(game:HttpGet('https://raw.githubusercontent.com/EdgeIY/infiniteyield/master/source'))()
  	end    
})

Tab:AddButton({
	Name = "Universal Script Hub (*UPDATED*)",
	Callback = function()
      		print("button pressed")
              loadstring(game:HttpGet("https://pastebin.com/raw/QSkQS1Xn",true))()
  	end    
})

local Tab = Window:MakeTab({
	Name = "Chat Bypasser⚠️⚠️⚠️",
	Icon = "rbxassetid://4483345998",
	PremiumOnly = false
})

Tab:AddButton({
	Name = "Menace Chat bypass v2.1",
	Callback = function()
      		print("button pressed")
              loadstring(game:HttpGet('https://raw.githubusercontent.com/Anonymous12131/MenaceV2.1.-Best-bypasser/main/obf_i7iAAH834YFQno0FLyuLPcNR5ouJrmvEFdtmXtF1C6u935ghB9DAFK5L3zgsPs6X.lua.txt'))()
  	end    
})

local Section = Tab:AddSection({
	Name = "READ THIS I RECOMMEN USING ALT⚠️⚠️⚠️"
})

local Tab = Window:MakeTab({
	Name = "Universal Guis",
	Icon = "rbxassetid://4483345998",
	PremiumOnly = false
})

Tab:AddButton({
	Name = "spy gui",
	Callback = function()
      		print("button pressed")
              loadstring(game:HttpGet("https://raw.githubusercontent.com/MichaelScripter/MiniScript/main/Spy%20gui.lua"))()
  	end    
})

local Tab = Window:MakeTab({
	Name = "Teleport",
	Icon = "rbxassetid://4483345998",
	PremiumOnly = false
})



Tab:AddToggle({
	Name = "Press Z To Teleport",
	Default = false,
	Callback = function(Value)
		print(Value)
        plr = game.Players.LocalPlayer
        
        hum = plr.Character.HumanoidRootPart
        
        mouse = plr:GetMouse()
        
        
        
        mouse.KeyDown:connect(function(key)
        
        if key == "z" then
        
        if mouse.Target then
        
        hum.CFrame = CFrame.new(mouse.Hit.x, mouse.Hit.y + 5, mouse.Hit.z)
        
        end
        
        end
        end)
	end    
})

local Tab = Window:MakeTab({
	Name = "Updates",
	Icon = "rbxassetid://5180300644",
	PremiumOnly = false
})

Tab:AddLabel("+Added Levitate Grab")

Tab:AddLabel("+Added Anti Burn")

Tab:AddLabel("+Added Anti Explosion")

Tab:AddLabel("+Added Anti Kick")

Tab:AddLabel("+Added Anti Void")

Tab:AddLabel("+Added Keybind Z Teleport")

Tab:AddLabel("+Stronger Throws Fixed")

Tab:AddLabel("+Added Noclip Grab")

Tab:AddLabel("+Added Annoy People")

Tab:AddLabel("+Added Bring Objects")

Tab:AddLabel("+Added Kill Grab")

Tab:AddLabel("+Fixed Anti Explosion And Anti Grab")

Tab:AddLabel("+Added A Fake Ban Its SAFE")

Tab:AddLabel("+Added Control Player ")

Tab:AddLabel("+Added Fov Changer ")

Tab:AddLabel("+Many Bugs FIXED")

Tab:AddLabel("Adding Help Option Later")

Tab:AddLabel("Updating today")

Tab:AddLabel("+Added Kick All coming soon..")

Tab:AddLabel("More Features Coming Soon")

Tab:AddLabel("VERBAL HUB ON TOP")

local Tab = Window:MakeTab({
	Name = "Help",
	Icon = "rbxassetid://10983517742",
	PremiumOnly = false
})

Tab:AddLabel("To TelePort Use Key Z ")

Tab:AddLabel("T Bring Object Use KeyBind T aim at the item u can get kicked if used incorrect")

Tab:AddLabel("To Use Kill Grab press F to turn on and turn off ")

Tab:AddLabel(" loop kill use they annoy option and kill grab")

Tab:AddLabel("Noclip Grab Broken being fixed")

local Tab = Window:MakeTab({
	Name = "kick aura beta",
	Icon = "rbxassetid://4483345998",
	PremiumOnly = false
})

Tab:AddButton({
	Name = "dont click still testing",
	Callback = function()
      		print("button pressed")
              local Players = game:GetService"Players"
              local Lp = Players.LocalPlayer
              Players.LocalPlayer.CameraMode = Enum.CameraMode.Classic
              game:GetService"RunService".Stepped:connect(function()
              pcall(function()
              for i, v in next, Lp.Character:GetDescendants() do
              if v:IsA"BasePart" and v.CanCollide then v.CanCollide = false end
              end
              end)
              end)
              while true do
              for i, v in next, Players:GetChildren() do
              pcall(function()
              local BodyVel = Lp.Character.HumanoidRootPart:FindFirstChild"BodyVelocity" or Instance.new("BodyVelocity", Lp.Character.HumanoidRootPart)
              BodyVel.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
              BodyVel.Velocity = Vector3.new(0, 0, 0)
              if v ~= Lp and v.Character and v.Character.HumanoidRootPart.CFrame.X > 200 then
              Lp.Character.HumanoidRootPart.CFrame = CFrame.new(v.Character.HumanoidRootPart.Position) * CFrame.new(0, -7, 0)
              task.wait(0.5)
              game:GetService"ReplicatedStorage".GrabEvents.SetNetworkOwner:FireServer(v.Character.HumanoidRootPart, "player")
              task.wait(0.5)
              v.Character.HumanoidRootPart.CFrame = CFrame.new(0, 5e3, 5e4)
              else
              Lp.Character.HumanoidRootPart.CFrame = workspace.SpawnLocation.CFrame
              end
              end)
              end
              pcall(function()
              Lp.Character.HumanoidRootPart.CFrame = workspace.SpawnLocation.CFrame
              end)
              task.wait(1)
              end
  	end    
})


OrionLib:MakeNotification({
	Name = "Welcome to Verbal Hub",
	Content = "hmmm i think im going to play FTAP and hack lol",
	Image = "rbxassetid://6675147490",
	Time = 10
})

Tab:AddLabel("Verbal Hub On Top")
